<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.nathansass.miro.MainActivity">
    <ImageView
        android:layout_alignParentTop="true"
        android:id="@+id/ivImage"
        android:layout_centerHorizontal="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/btnLoadResource"
        android:text="@string/load_images"
        android:layout_alignParentBottom="true"/>
    <Button
        android:id="@+id/btnClearFileCache"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/clear_file_cache"
        android:layout_above="@id/btnLoadResource"/>
    <Button
        android:id="@+id/btnClearMemoryCache"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/clear_memory_cache"
        android:layout_above="@id/btnClearFileCache"/>
</RelativeLayout>
